{
  "rules": [
    {
      "id": "javascript/syntax",
      "kind": "parse-errors",
      "severity": "error",
      "message": "file does not parse (editor-level syntax check \u2014 a compiler verdict belongs to the language's own compiler)"
    }
  ]
}
