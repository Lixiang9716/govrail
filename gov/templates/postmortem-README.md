# Postmortems

One file per incident worth remembering: what happened, what it cost,
the contributing causes, and the change that keeps it from recurring.

A postmortem is memory, not blame — names appear only as witnesses and
fix-owners. `gov recall` searches these files alongside notes and the
decision log, so an agent planning similar work finds the scar tissue
before it repeats it.
